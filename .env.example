# Server Configuration
PORT=3000
NODE_ENV=development

# Authentication
# Generate a secure random token for production use
# You can use: openssl rand -base64 32
API_TOKEN=your-secret-token-here

# CORS Configuration
# Set specific origins for production, e.g.: https://yourdomain.com
# Use * for development only
CORS_ORIGIN=*

# Rate Limiting
# Maximum number of requests per 15 minutes per IP
RATE_LIMIT_MAX=100
